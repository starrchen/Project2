<div class="jumbotron vertical-center">
  <h2>Menu Options <%= link_to "(+)", new_restaurant_menu_path(@restaurant) %></h2>

  <ul>
    <%@menus.each do |menu| %>
    <li>
      <%=link_to menu.name, edit_restaurant_menu_path(@restaurant, menu) %>
    </li>
    <%end%>
  </ul>
</div>
